namespace Art.Web.Server.Filters
{
    public struct CachingProfiles
    {
        public const string Caching3600 = nameof(Caching3600);

        public const string NoCaching = nameof(NoCaching);
    }
}
